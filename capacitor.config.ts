import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sse8ball-frontend',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
